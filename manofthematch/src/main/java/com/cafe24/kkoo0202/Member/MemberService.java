package com.cafe24.kkoo0202.Member;

import javax.servlet.http.HttpSession;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.cafe24.kkoo0202.Member.MemberDao;
import com.cafe24.kkoo0202.Member.Member;

@Service
public class MemberService {
	
	
		
		
		
	
	/*
	@Autowired
	HttpSession session; 
	 
	
	
	
	 회원 등록 
	public void MemberAddPro(Member member) {
		if(member.geteLanguage().equals("")) { \
				member.seteLanguage(null); 
				} 
			Login result = (Login)session.getAttribute("loginfor"); 
			Member.setmemberId(result.getmemberId()); 
			MemberDao.MemberAddPro(member); 
	*/
		
		
	}


